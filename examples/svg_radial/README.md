# svg_radial

Six tiles render the same 100x100 box with linear, centered radial,
off-center radial, large-R radial, multi-stop radial, and a
shifted-focal radial gradient. Confirms the new
`<radialGradient>` parsing + render path.

## Run

```
go run ./examples/svg_radial
```
