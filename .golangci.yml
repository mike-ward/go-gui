linters:
  enable:
    - govet
    - staticcheck
    - errcheck
    - gosimple
    - unused
    - gofmt
    - goimports
    - revive

issues:
  exclude-use-default: false
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
